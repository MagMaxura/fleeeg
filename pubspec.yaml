name: fleteen_flutter
description: "A premium Flutter application for Fleteen, a native flete marketplace."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Backend & Supabase
  supabase_flutter: ^2.6.0
  flutter_secure_storage: ^9.0.0

  # State Management & Routing
  flutter_riverpod: ^2.5.0
  go_router: ^14.0.0

  # Maps & Location
  google_maps_flutter: ^2.6.0
  geolocator: ^12.0.0

  # UI and Media
  cached_network_image: ^3.3.0
  image_picker: ^1.1.0
  audioplayers: ^6.0.0
  url_launcher: ^6.2.5

  # Design & Styling Helpers
  flutter_blurhash: ^0.8.2
  shimmer: ^3.0.0
  intl: ^0.19.0
  font_awesome_flutter: ^10.7.0

  # Notifications
  flutter_local_notifications: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/images/
  #   - assets/sounds/
